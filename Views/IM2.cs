using System.Windows;

namespace AeonHacs.Wpf.Views;

public class IM2 : Section
{
    static IM2()
    {
        DefaultStyleKeyProperty.OverrideMetadata(typeof(IM2), new FrameworkPropertyMetadata(typeof(IM2)));
    }
}
